# Claude Code local config — never pushed, per team instruction
.claude/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.uv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/

# Node / JS
node_modules/
.turbo/
.next/
dist/
build/
*.tsbuildinfo
.expo/

# Env and secrets
.env
.env.*
!.env.example

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log
artifacts/

# Storybook build output (file 19). Regenerated by `pnpm --filter @sarana/ui storybook:build`.
storybook-static/

# Generated by `next build`. It carries a reference to .next/types, so committing it
# means a diff on every build. Next's own default gitignore excludes it too.
next-env.d.ts

# Playwright output (file 20).
test-results/
playwright-report/
