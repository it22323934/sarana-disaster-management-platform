"""gov-mock owns no schema in docs/build-prompts/04-data-model.md — it mocks external
systems rather than persisting SARANA domain data. Kept as an empty placeholder only for
skeleton consistency with the other five services; it may end up unused.
"""
