[project]
name = "sarana-agent-svc"
version = "0.1.0"
description = "SARANA Agent Service - The LangGraph runtime hosting the six SARANA agents, their durable state and the human-in-the-loop interrupts"
requires-python = ">=3.12,<3.13"
dependencies = [
    "sarana-shared",
    "uvicorn[standard]>=0.34",
    "httpx>=0.28",
]

[dependency-groups]
dev = [
    "sarana-shared[testing]",
    "pytest>=8.3",
    "pytest-asyncio>=0.25",
    "pytest-cov>=6.0",
    "httpx>=0.28",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_svc"]

[tool.uv.sources]
sarana-shared = { workspace = true }
