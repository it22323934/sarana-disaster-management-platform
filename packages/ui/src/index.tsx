/**
 * The SARANA shared component library.
 *
 * The design system itself - tokens, typography for three scripts, the map shell, the
 * two human-gate dialogs - is built in a later step. This package exists now so that
 * both web apps and the mobile app depend on one component source from the first commit
 * rather than diverging and being reconciled later.
 */

export { SimulatedDataBadge } from './simulated-data-badge.js';
export type { SimulatedDataBadgeProps } from './simulated-data-badge.js';
