"""Shared JWT verification (RS256, local against JWKS) and the RBAC scope model.

Token minting, login/refresh flows, TOTP, and offline capability tokens are built out in
docs/build-prompts/05-auth-rbac.md, inside core-api — not in this shared package.
"""
