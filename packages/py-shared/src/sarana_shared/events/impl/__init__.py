"""Concrete EventBus implementations.

Only `in_memory.py` is built at scaffold stage, purely to give the shared test fixtures
(sarana_shared.testing) something real to hand tests. `redis_streams.py` (dev) and
`eventbridge.py` (AWS) are docs/build-prompts/06-event-bus.md's job.
"""
